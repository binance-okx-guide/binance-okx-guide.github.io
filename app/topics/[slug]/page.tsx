import type { Metadata } from "next";
import Link from "next/link";
import {articles,getTopic,topics} from "@/data/content";
export function generateStaticParams(){return topics.map(t=>({slug:t.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const t=getTopic(slug);return{title:`${t?.name??"新手教程"}｜2026完整指南`,description:t?.description}}
export default async function TopicPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const t=getTopic(slug)??topics[0];const list=articles.filter(a=>a.topic===t.slug);return <main><header className="sub-header"><Link className="brand" href="/"><span className="brand-mark">陈</span><span>币圈老陈</span></Link><span className="breadcrumb"><Link href="/">首页</Link> › {t.name}</span></header><section className="topic-hero"><span className="article-label">2026新手实操教程</span><h1>{t.name}</h1><p>{t.description} 我按新手搜索最多的问题整理，找到你卡住的那一步直接看。</p><div className="topic-list">{list.map((a,i)=><Link href={`/articles/${a.slug}`} key={a.slug}><span>{String(i+1).padStart(2,"0")}</span><div><h2>{a.title}</h2><p>{a.description}</p><small>{a.keywords.join(" · ")}</small></div><b>→</b></Link>)}</div></section><footer className="footer compact-footer"><p>老陈个人实操笔记 · 平台规则可能变化，请以官方当前页面为准</p></footer></main>}
